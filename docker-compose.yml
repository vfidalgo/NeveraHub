version: '3.8'

services:
  neverahub:
    build: .
    container_name: neverahub-fridge
    restart: unless-stopped
    ports:
      - "3030:3030"
    environment:
      - PORT=3030
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
